import { LotteriePipe } from './name-lotterie.pipe';

describe('NameLotteriePipe', () => {
  it('create an instance', () => {
    const pipe = new LotteriePipe();
    expect(pipe).toBeTruthy();
  });
});
